uploads:
  max_size_mb: 50
  concurrent: 3
  chunk_size_mb: 5
  allowed_extensions:
    - .pdf
    - .docx
    - .xlsx
    - .png
  scan:
    enabled: true
    provider: clamav
  quarantine_prefix: quarantine/
